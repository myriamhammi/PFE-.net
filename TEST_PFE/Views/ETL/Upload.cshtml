@using TEST_PFE.Extensions
@* @{
    ViewData["Title"] = "Upload ETL";
}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload ETL</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }

        h2 {
            color: #333;
        }

        form {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: 0 auto;
        }

        label {
            margin-bottom: 5px;
            font-weight: bold;
            display: block;
        }

        select, input[type="file"] {
            margin-bottom: 20px;
            padding: 10px;
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 1rem;
        }

        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
        }

            button:hover {
                background-color: #0056b3;
            }

        .message {
            margin-top: 20px;
            padding: 15px;
            border-radius: 5px;
            text-align: center;
        }

        .success {
            background-color: #d4edda;
            border-color: #c3e6cb;
            color: #155724;
        }

        .error {
            background-color: #f8d7da;
            border-color: #f5c6cb;
            color: #721c24;
        }
    </style>
</head>
<body>
    <h2>Lancer un processus ETL</h2>

    <form method="post" enctype="multipart/form-data" action="/ETL/UploadFile">
        @Html.AntiForgeryToken()

        <label for="selectedTable">Choisir la table :</label>
        <select id="selectedTable" name="selectedTable" required onchange="toggleFileInput()">
            <option value="" disabled selected>-- Sélectionnez une table --</option>
            <option value="Toutes">Toutes les tables</option>
            <option value="Prospect">Prospect</option>
            <option value="Compte">Account</option>
            <option value="Contact">Contact</option>
            <option value="Opportunity">Opportunities</option>
            <option value="Quote">Devis</option>
            <option value="Sales">Sales_Order</option>
            <option value="QuoteProduct">Quote_Product</option>
            <option value="OpportunityProduct">Opportunity_Product</option>
            <option value="SalesProduct">Sales_Order_Product</option>
        </select>

        <label for="uploadedFile">Fichier CSV :</label>
        <input id="uploadedFile" type="file" name="uploadedFile" accept=".csv" required />

        <button type="submit">Lancer l'ETL</button>
    </form>

    @if (ViewBag.Message != null)
    {
        <div class="message @(ViewBag.Message.Contains("Erreur") ? "error" : "success")">
            <strong>@ViewBag.Message</strong>
        </div>
    }

    <script>
        function toggleFileInput() {
            const selectedTable = document.getElementById("selectedTable").value;
            const fileInput = document.getElementById("uploadedFile");

            if (selectedTable === "Toutes") {
                fileInput.disabled = true;
                fileInput.removeAttribute("required");
            } else {
                fileInput.disabled = false;
                fileInput.setAttribute("required", "required");
            }
        }
    </script>

    <script src="/_framework/aspnetcore-browser-refresh.js"></script>
</body>
</html> *@

@* @{
    ViewData["Title"] = "Upload ETL";
    Layout = "~/Pages/Shared/_KPIsLayout.cshtml"; // Même layout que ta vue descriptive
} *@

<!-- Font Awesome -->
@* <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

<section class="py-5 bg-light">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="fw-bold text-uppercase">
                <i class="fas fa-database me-2" style="color: #FFE600;"></i> Lancer le processus ETL
            </h2>
            <p class="text-muted">Importe un fichier CSV pour lancer le traitement et alimenter les données analytiques.</p>
        </div>

        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="card shadow-sm border-0">
                    <div class="card-body">
                        <form method="post" enctype="multipart/form-data" action="/ETL/UploadFile">
                            @Html.AntiForgeryToken()

                            <div class="mb-3">
                                <label for="selectedTable" class="form-label fw-semibold">
                                    <i class="fas fa-table me-1" style="color: #FFE600;"></i> Choisir la table :
                                </label>
                                <select id="selectedTable" name="selectedTable" class="form-select" required onchange="toggleFileInput()">
                                    <option value="" disabled selected>-- Sélectionnez une table --</option>
                                    <option value="Toutes">Toutes les tables</option>
                                    <option value="Prospect">Prospect</option>
                                    <option value="Compte">Account</option>
                                    <option value="Contact">Contact</option>
                                    <option value="Opportunity">Opportunities</option>
                                    <option value="Quote">Devis</option>
                                    <option value="Sales">Sales_Order</option>
                                    <option value="QuoteProduct">Quote_Product</option>
                                    <option value="OpportunityProduct">Opportunity_Product</option>
                                    <option value="SalesProduct">Sales_Order_Product</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label for="uploadedFile" class="form-label fw-semibold">
                                    <i class="fas fa-file-csv me-1" style="color: #FFE600;"></i> Fichier CSV :
                                </label>
                                <input id="uploadedFile" type="file" name="uploadedFile" accept=".csv" class="form-control" required />
                            </div>

                            <div class="d-grid">
                                <button id="submitBtn" type="submit" class="btn btn-warning text-dark fw-bold" disabled>
                                    <span id="btnText"><i class="fas fa-play me-2"></i> Lancer l'ETL</span>
                                    <span id="btnSpinner" class="spinner-border spinner-border-sm d-none ms-2" role="status" aria-hidden="true"></span>
                                </button>
                            </div>

                        </form>

                        @if (ViewBag.Message != null)
                        {
                            <div class="alert mt-4 @(ViewBag.Message.Contains("Erreur") ? "alert-danger" : "alert-success") text-center fw-semibold">
                                @ViewBag.Message
                            </div>
                        }

                        @if (ViewBag.PackageOutput != null)
                        {
                            <div class="alert alert-secondary mt-3">
                                <strong>Sortie du package :</strong>
                                <pre>@ViewBag.PackageOutput</pre>
                            </div>
                        }

                        @if (ViewBag.PackageError != null)
                        {
                            <div class="alert alert-danger mt-3">
                                <strong>Erreurs du package :</strong>
                                <pre>@ViewBag.PackageError</pre>
                            </div>
                        } *@

                        @* FICHIERS RÉCENTS REUTILISABLES *@
                       @*  @if (ViewBag.RecentFiles != null && (ViewBag.RecentFiles as List<string>).Any())
                        {
                            <div class="card mt-4 shadow-sm">
                                <div class="card-header bg-primary text-white">
                                    <h5 class="mb-0">📂 Fichiers récemment téléchargés</h5>
                                </div>
                                <ul class="list-group list-group-flush">
                                    @foreach (var file in ViewBag.RecentFiles as List<string>)
                                    {
                                        <li class="list-group-item d-flex justify-content-between align-items-center">
                                            <span><i class="bi bi-file-earmark-arrow-down-fill me-2 text-success"></i>@file</span>
                                            <a href="@Url.Content($"wwwroot/uploads/{file}")" class="btn btn-outline-primary btn-sm" target="_blank">
                                                Télécharger
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </div>
                        }


                    </div>
                </div>
            </div>
        </div>
    </div>
</section> *@

@* @section Scripts {
    <script>
        function toggleFileInput() {
            const selectedTable = document.getElementById("selectedTable").value;
            const fileInput = document.getElementById("uploadedFile");
            const submitBtn = document.getElementById("submitBtn");

            if (selectedTable === "Toutes") {
                fileInput.disabled = true;
                fileInput.removeAttribute("required");
            } else {
                fileInput.disabled = false;
                fileInput.setAttribute("required", "required");
            }

            submitBtn.disabled = selectedTable === "";
        }

         document.addEventListener("DOMContentLoaded", () => {
            const form = document.querySelector("form");
            const btnText = document.getElementById("btnText");
            const btnSpinner = document.getElementById("btnSpinner");
            const submitBtn = document.getElementById("submitBtn");

            form.addEventListener("submit", function () {
                btnText.innerText = "Traitement en cours...";
                btnSpinner.classList.remove("d-none");
                submitBtn.disabled = true;
            });
        });


        function prefillForm(fileName, tableName) {
             const tableSelect = document.getElementById("selectedTable");
             const fileInput = document.getElementById("uploadedFile");

             for (let option of tableSelect.options) {
                 if (option.value.toLowerCase() === tableName.toLowerCase()) {
                     option.selected = true;
                     break;
                 }
             }

             toggleFileInput();
             alert(`Veuillez sélectionner manuellement le fichier : ${fileName} dans votre système.`);
             document.getElementById("submitBtn").disabled = false;
         }

    </script>
}
 *@

@{
    ViewData["Title"] = "Upload ETL";
    Layout = "~/Pages/Shared/_KPIsLayout.cshtml"; // Même layout que ta vue descriptive
}

<!-- Font Awesome -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

<section class="py-5 bg-light" style="min-height: 100vh;">
    <div class="container-fluid px-5">
        <div class="text-center mb-5">
            <h2 class="fw-bold text-uppercase">
                <i class="fas fa-database me-2" style="color: #FFE600;"></i> Upload & Lancement du systeme ETL
            </h2>
            <p class="text-muted">Séléctionnez une table, importez un fichier CSV , et déclenchez le traitement.</p>
        </div>

        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="card shadow-sm border-0">
                    <div class="card-body">
                        <form method="post" enctype="multipart/form-data" action="/ETL/UploadFile">
                            @Html.AntiForgeryToken()

                            <div class="mb-3">
                                <label for="selectedTable" class="form-label fw-semibold">
                                    <i class="fas fa-table me-1" style="color: #FFE600;"></i> Choisir la table :
                                </label>
                                <select id="selectedTable" name="selectedTable" class="form-select" required onchange="toggleFileInput()">
                                    <option value="" disabled selected>-- Sélectionnez une table --</option>
                                    <option value="Toutes">Toutes les tables</option>
                                    <option value="Prospect">Prospect</option>
                                    <option value="Compte">Account</option>
                                    <option value="Contact">Contact</option>
                                    <option value="Opportunity">Opportunities</option>
                                    <option value="Quote">Devis</option>
                                    <option value="Sales">Sales_Order</option>
                                    <option value="QuoteProduct">Quote_Product</option>
                                    <option value="OpportunityProduct">Opportunity_Product</option>
                                    <option value="SalesProduct">Sales_Order_Product</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label for="uploadedFile" class="form-label fw-semibold">
                                    <i class="fas fa-file-csv me-1" style="color: #FFE600;"></i> Fichier CSV :
                                </label>
                                <input id="uploadedFile" type="file" name="uploadedFile" accept=".csv" class="form-control" required />
                            </div>

                            <div class="d-grid">
                                <button id="submitBtn" type="submit" class="btn btn-warning text-dark fw-bold" disabled>
                                    <span id="btnText"><i class="fas fa-play me-2"></i> Lancer l'ETL</span>
                                    <span id="btnSpinner" class="spinner-border spinner-border-sm d-none ms-2" role="status" aria-hidden="true"></span>
                                    <!-- Barre de progression en jaune -->
                                    <div class="progress mt-2 d-none" id="progressBar" style="height: 5px;">
                                        <div class="progress-bar progress-bar-striped progress-bar-animated" style="width: 0%; background-color: #FFE600;" id="progress"></div>
                                    </div>
                                </button>
                            </div>

                        </form>

                        @if (ViewBag.Message != null)
                        {
                            <div class="alert mt-4 @(ViewBag.Message.Contains("Erreur") ? "alert-danger" : "alert-success") text-center fw-semibold">
                                @ViewBag.Message
                            </div>
                        }

                        @if (ViewBag.PackageOutput != null)
                        {
                            <div class="alert alert-secondary mt-3">
                                <strong>Sortie du package :</strong>
                                <pre>@ViewBag.PackageOutput</pre>
                            </div>
                        }

                        @if (ViewBag.PackageError != null)
                        {
                            <div class="alert alert-danger mt-3">
                                <strong>Erreurs du package :</strong>
                                <pre>@ViewBag.PackageError</pre>
                            </div>
                        }

                        @* FICHIERS RÉCENTS REUTILISABLES *@
                        @if (ViewBag.RecentFiles != null && (ViewBag.RecentFiles as List<string>).Any())
                        {
                            <div class="card mt-4 shadow-sm border-0">
                                <div class="card-header d-flex align-items-center" style="background-color: #FFE600;">
                                    <i class="fas fa-folder-open me-2 text-dark"></i>
                                    <h5 class="mb-0 text-dark fw-bold">Fichiers récemment téléchargés</h5>
                                </div>
                                <ul class="list-group list-group-flush">
                                    @foreach (var file in ViewBag.RecentFiles as List<string>)
                                    {
                                        <li class="list-group-item d-flex justify-content-between align-items-center">
                                            <div class="d-flex align-items-center">
                                                <i class="fas fa-file-csv me-2 text-success"></i>
                                                <span class="fw-semibold">@file</span>
                                            </div>
                                            <a href="@Url.Content("~/uploads/" + file)"
                                               class="btn btn-sm fw-bold text-dark"
                                               style="background-color: #FFE600; border: none;"
                                               target="_blank">
                                                Télécharger
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </div>
                        }


                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        function toggleFileInput() {
            const selectedTable = document.getElementById("selectedTable").value;
            const fileInput = document.getElementById("uploadedFile");
            const submitBtn = document.getElementById("submitBtn");

            if (selectedTable === "Toutes") {
                fileInput.disabled = true;
                fileInput.removeAttribute("required");
            } else {
                fileInput.disabled = false;
                fileInput.setAttribute("required", "required");
            }

            submitBtn.disabled = selectedTable === "";
        }

         document.addEventListener("DOMContentLoaded", () => {
            const form = document.querySelector("form");
            const btnText = document.getElementById("btnText");
            const btnSpinner = document.getElementById("btnSpinner");
            const submitBtn = document.getElementById("submitBtn");
            const progressBar = document.getElementById("progressBar");
            const progress = document.getElementById("progress");

            form.addEventListener("submit", function () {
                btnText.innerText = "Traitement en cours...";
                btnSpinner.classList.remove("d-none");
                progressBar.classList.remove("d-none");
                submitBtn.disabled = true;

                // Simuler une progression pour le test
                let progressValue = 0;
                let interval = setInterval(function () {
                    progressValue += 10;
                    progress.style.width = progressValue + "%";
                    if (progressValue >= 100) {
                        clearInterval(interval);
                    }
                }, 1000);
            });
        });

        function prefillForm(fileName, tableName) {
             const tableSelect = document.getElementById("selectedTable");
             const fileInput = document.getElementById("uploadedFile");

             for (let option of tableSelect.options) {
                 if (option.value.toLowerCase() === tableName.toLowerCase()) {
                     option.selected = true;
                     break;
                 }
             }

             toggleFileInput();
             alert(`Veuillez sélectionner manuellement le fichier : ${fileName} dans votre système.`);
             document.getElementById("submitBtn").disabled = false;
         }

    </script>
}


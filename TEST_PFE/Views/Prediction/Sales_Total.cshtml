@{
    ViewData["Title"] = "Prédiction Montant total de la commande";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container mt-5">
    <h2 class="mb-4">Estimation du montant total de la commande</h2>

    <div id="formsContainer">
        <!-- Premier formulaire (prototype à dupliquer) -->
        <div class="prediction-form card shadow-lg p-4 mb-4">
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">Nom du client</label>
                    <select class="form-select client_name" onchange="toggleCustomClient(this)">
                        <option disabled selected>Choisissez un client</option>
                        <option value="__custom__">➕ Nouveau client</option>
                    </select>
                    <input type="text" class="form-control custom_client_name d-none" placeholder="Entrez le nom complet du client" />
                </div>

                <div class="col-md-6">
                    <label class="form-label">Secteur d’activité</label>
                    <select class="form-select industry">
                        <option>Technology</option>
                        <option>Finance</option>
                        <option>Healthcare</option>
                        <option>Retail</option>
                        <option>Education</option>
                        <option>Manufacturing</option>
                        <option>Real Estate</option>
                    </select>
                </div>

                <div class="col-md-6">
                    <label class="form-label">Revenu potentiel (TND)</label>
                    <input type="range" class="form-range revenue" min="1000" max="200000" step="1000" oninput="this.nextElementSibling.value = this.value" />
                    <output class="revenueOutput">1000</output>
                </div>

                <div class="col-md-6">
                    <label class="form-label">Montant du devis (TND)</label>
                    <input type="number" class="form-control quote_total" placeholder="75000" />
                </div>

                <div class="col-md-6">
                    <label class="form-label">Étape de l'opportunité</label>
                    <select class="form-select stage">
                        <option>Negotiation</option>
                        <option>Closed Won</option>
                        <option>Closed Lost</option>
                    </select>
                </div>

                <div class="col-md-6">
                    <label class="form-label">Produit concerné</label>
                    <select class="form-select product">
                        <option>ERP System</option>
                        <option>Business Analytics</option>
                        <option>Custom Development</option>
                        <option>Cybersecurity Suite</option>
                        <option>IT Support</option>
                        <option>Cloud Hosting</option>
                        <option>CRM Software</option>
                    </select>
                </div>

                <div class="col-12 d-flex justify-content-between mt-3">
                    <button class="btn btn-danger d-none remove-btn" onclick="removeForm(this)">🗑️ Supprimer ce formulaire</button>
                    <button class="btn btn-primary" onclick="makePrediction(this)">Lancer la prédiction</button>
                </div>

            </div>
        </div>
    </div>

    <button id="addFormBtn" type="button" class="btn btn-warning mt-3 fw-bold" onclick="addForm()">
        ➕ Ajouter un formulaire de prédiction
    </button>

    <div id="resultsContainer" class="mt-4"></div>
    <button class="btn btn-secondary mt-3" onclick="getPredictionsOverview()">📊 Voir les prédictions globales</button>



    <!-- Bouton global pour lancer toutes les prédictions si nécessaire -->
    <!-- <button id="predictBtn" class="btn btn-success mt-4" onclick="makeAllPredictions()">Lancer toutes les prédictions</button> -->
</div>

<script>
    async function loadClients(selectElement) {
        try {
            const response = await fetch("http://127.0.0.1:5001/get_clients");
            const clients = await response.json();

            selectElement.innerHTML = `
                <option disabled selected>Choisissez un client</option>
                <option value="__custom__">➕ Nouveau client</option>
            `;
            clients.forEach(client => {
                const option = document.createElement("option");
                option.value = client;
                option.text = client;
                selectElement.appendChild(option);
            });
        } catch (error) {
            console.error("Erreur lors du chargement des clients:", error);
        }
    }

    function toggleCustomClient(select) {
        const container = select.closest(".prediction-form");
        const customInput = container.querySelector(".custom_client_name");
        customInput.classList.toggle("d-none", select.value !== "__custom__");
        if (select.value === "__custom__") customInput.focus();
    }

    function addForm() {
        const container = document.getElementById("formsContainer");
        const template = container.querySelector(".prediction-form");
        const clone = template.cloneNode(true);

        // Reset inputs
        clone.querySelectorAll("input, select, output").forEach(el => {
            if (el.tagName === "SELECT") {
                if (el.classList.contains("client_name")) {
                    loadClients(el);
                } else {
                    el.selectedIndex = 0;
                }
            } else if (el.type === "range") {
                el.value = 1000;
                el.nextElementSibling.value = 1000;
            } else {
                el.value = "";
            }
        });

        // Afficher bouton de suppression
        const removeBtn = clone.querySelector(".remove-btn");
        removeBtn.classList.remove("d-none");


        container.appendChild(clone);
    }

    function removeForm(button) {
        const form = button.closest(".prediction-form");
        form.remove();
    }

    async function makePrediction(button) {
        const form = button.closest(".prediction-form");
        const clientSelect = form.querySelector(".client_name");
        const customClientInput = form.querySelector(".custom_client_name");
        const clientName = clientSelect.value === "__custom__" ? customClientInput.value : clientSelect.value;
        const industry = form.querySelector(".industry").value;
        const stage = form.querySelector(".stage").value;
        const product = form.querySelector(".product").value;
        const revenue = parseFloat(form.querySelector(".revenue").value);
        const quote = parseFloat(form.querySelector(".quote_total").value);

        if (!clientName || isNaN(revenue) || isNaN(quote)) {
            alert("Veuillez remplir tous les champs correctement.");
            return;
        }

        const payload = {
            Full_Name: clientName,
            client_name: clientName,
            customer_industry: industry,
            opportunity_stage: stage,
            product_name: product,
            Revenue_Potential: revenue,
            Quote_Total: quote
        };

        try {
            const response = await fetch("http://127.0.0.1:5001/predict", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify(payload)
            });

            const result = await response.json();
            const resultsContainer = document.getElementById("resultsContainer");

            if (response.ok) {
                resultsContainer.innerHTML += `
                    <div class="alert alert-info mt-3">
                        <h5>Résultat pour ${clientName}</h5>
                        <strong>Modèle utilisé :</strong> ${result.model}<br/>
                        <strong>Statut client :</strong> ${result.client_status}<br/>
                        <strong>Montant estimé :</strong> ${result.prediction.toLocaleString()} TND
                    </div>
                `;
            } else {
                resultsContainer.innerHTML += `<div class="alert alert-danger mt-3">Erreur pour ${clientName} : ${result.error || result.message}</div>`;
            }
        } catch (error) {
            document.getElementById("resultsContainer").innerHTML += `<div class="alert alert-danger mt-3">Erreur réseau pour ${clientName} : ${error.message}</div>`;
        }
    }


     let previousOverview = null; // Stockage de la dernière vue d'ensemble

        function formatEvolution(oldValue, newValue) {
        if (oldValue === null || oldValue === undefined) return "";
        const diff = newValue - oldValue;
        const sign = diff > 0 ? "+" : "";
        return ` <span style="color:${diff > 0 ? 'green' : (diff < 0 ? 'red' : 'black')}">(${sign}${diff.toLocaleString('fr-FR')})</span>`;
    }

    // Pour comparer les répartitions (objets) par secteur ou étape
    function formatDistributionEvolution(oldDist, newDist) {
        if (!oldDist || !newDist) return "";
        let changes = [];
        for (const key of new Set([...Object.keys(oldDist), ...Object.keys(newDist)])) {
            const oldVal = oldDist[key] ?? 0;
            const newVal = newDist[key] ?? 0;
            if (oldVal !== newVal) {
                const diff = newVal - oldVal;
                const sign = diff > 0 ? "+" : "";
                changes.push(`${key} : <span style="color:${diff > 0 ? 'green' : 'red'}">${sign}${diff}</span>`);
            }
        }
        return changes.length > 0 ? changes.join(", ") : "Aucune évolution";
    }


        async function getPredictionsOverview() {
        try {
            const response = await fetch("http://127.0.0.1:5001/get_predictions_overview");
            const data = await response.json();

            console.log("📦 Données reçues :", data);

            const container = document.getElementById("resultsContainer");

            // Affichage avec comparaison à la donnée précédente
                container.innerHTML = `
        <div class="alert alert-secondary mt-4">
            <h4 class="mb-3">📊 Vue d'ensemble des prédictions</h4>

            <p><strong>Total des prédictions :</strong> ${data.total_predictions ?? "N/A"}${previousOverview ? formatEvolution(previousOverview.total_predictions, data.total_predictions) : ""}</p>

            <p><strong>Moyenne des montants estimés :</strong> ${data.average_prediction?.toLocaleString('fr-FR') ?? "N/A"} TND${previousOverview ? formatEvolution(previousOverview.average_prediction, data.average_prediction) : ""}</p>

            <h5 class="mt-4">📌 Répartition par secteur d’activité :</h5>
            <ul>
                ${data.industry_distribution
                    ? Object.entries(data.industry_distribution).map(([k, v]) => `<li>${k} : ${v}</li>`).join("")
                    : "<li>Aucune donnée</li>"}
            </ul>
            ${previousOverview
                ? `<small>${formatDistributionEvolution(previousOverview.industry_distribution, data.industry_distribution)}</small>`
                : ""}

            <h5 class="mt-4">📌 Répartition par étape :</h5>
            <ul>
                ${data.stage_distribution
                    ? Object.entries(data.stage_distribution).map(([k, v]) => `<li>${k} : ${v}</li>`).join("")
                    : "<li>Aucune donnée</li>"}
            </ul>
            ${previousOverview
                ? `<small>${formatDistributionEvolution(previousOverview.stage_distribution, data.stage_distribution)}</small>`
                : ""}

            <h5 class="mt-4">🕒 Dernières prédictions :</h5>
            <ul>
                ${Array.isArray(data.latest_predictions)
                    ? data.latest_predictions.map(pred => `
                        <li>
                            <strong>${pred.client} (${pred.industry})</strong> - ${pred.product}, ${pred.stage} →
                            <strong>${pred.prediction.toLocaleString('fr-FR')} TND</strong> (${pred.created_at})
                        </li>
                    `).join("")
                    : "<li>Aucune prédiction disponible</li>"}
            </ul>

            <h5 class="mt-4">📈 Évolution dans le temps :</h5>
            <ul>
                ${Array.isArray(data.evolution_overview) && data.evolution_overview.length > 0
                    ? data.evolution_overview.map(e => `
                        <li>
                            <strong>${e.client} (${e.industry})</strong> - ${e.product}, ${e.stage} →
                            <strong>${e.latest_prediction.toLocaleString('fr-FR')} TND</strong>
                            ${e.previous_prediction !== null ? `(précédente : ${e.previous_prediction.toLocaleString('fr-FR')} TND)` : ""}
                            ${e.evolution !== null ? `📈 Évolution : ${e.evolution.toLocaleString('fr-FR')} TND` : ""}
                            (${e.created_at})
                        </li>
                    `).join("") : "<li>Aucune évolution disponible</li>"}
            </ul>
        </div>
    `;


            // Met à jour previousOverview pour la prochaine comparaison
            previousOverview = data;

        } catch (error) {
            console.error("❌ Erreur fetch :", error);
              document.getElementById("resultsContainer").innerHTML = `
        <div class="alert alert-danger mt-3">
            ❌ Erreur lors de la récupération des prédictions globales : ${error.message}
        </div>
    `;

        }
    }

    // Pour rafraîchir automatiquement toutes les 10 secondes (ou après un event spécifique), tu peux appeler régulièrement :
    setInterval(getPredictionsOverview, 10000);



    // Initialisation au chargement de la page
    window.onload = () => {
        const firstSelect = document.querySelector(".client_name");
        loadClients(firstSelect);
    };
</script>
